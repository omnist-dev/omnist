## Summary

Mechanical type-annotation work: fix all 75 `mypy --strict` errors (missing type args on generics, untyped defs) with two small invariant restructures using assertions. Add `typecheck` job to CI workflow. Bump version 0.2.24 -> 0.2.25.

## Details

- `mypy --strict omnist` now exits cleanly (0 errors, 19 source files)
- Two invariant restructures made locally provable:
  - `omnist/ops/extract.py`: `first_offender` annotated as `Optional[tuple[str, str]]` with assertion before unpack
  - `omnist/cli.py`: `report` narrowed with `assert report is not None` under `if args.report:`
- CI workflow: added `typecheck` job (Python 3.12) running `mypy --strict omnist`
- `pyproject.toml`: added `mypy` to `dev` extras
- Version: 0.2.24 -> 0.2.25
- CHANGELOG and docs/testing.md updated

## Test plan

- `python3 -m mypy --strict omnist` exits 0 ✓
- `python3 -m pytest -q` passes all 609 tests ✓
- `ruff check omnist tests examples tools` clean ✓
- Coverage: 100% on omnist/* maintained ✓
- Git diff contains only type annotations, two asserts, CI config, version, and docs ✓
